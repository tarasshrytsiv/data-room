[build]
builder = "nixpacks"
buildCommand = "cd ../.. && npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter api build"

[deploy]
startCommand = "node dist/main"
restartPolicyType = "on_failure"
